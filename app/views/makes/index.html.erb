<%= form_tag models_path, method: :get  do %>
  <p>
    <%= label_tag(:id, 'Escolha a Fabricante:') %>
    <%= select_tag :make_select, options_from_collection_for_select(@makes, :id, :name),{:class => 'form-control'} %>
  </p>
    <%= submit_tag "Buscar modelos", {:class => 'btn btn-primary'} %>
<% end %>
